---
layout: ../../../../layouts/DocsLayout.astro
title: BP-CMPLX-001 Graph Complexity | Blueprint Health Analyzer
description: Learn why complex Blueprint graphs hurt maintainability and how to refactor them effectively.
---

import ExampleToggle from '../../../../components/ExampleToggle.astro';
import SeverityBadge from '../../../../components/SeverityBadge.astro';
import { bpCmplxRuleExamples } from '../../../../data/ruleBlueprintExamples';

# BP-CMPLX-001

<div class="rule-meta">
  <SeverityBadge severity="Warning" />
  <span class="category">Blueprint</span>
  <span class="rule-title">Graph Complexity</span>
</div>

<div class="rule-summary">
Flags Blueprints with high combined complexity. This is about human performance—maintainability, reviewability, and defect risk—not frame time.
</div>

<aside class="rule-quick">
  <div class="rule-quick__section">
    <h4>Threshold</h4>
    <code>300</code> complexity score
  </div>
  <div class="rule-quick__section">
    <h4>Measure</h4>
    Node count × branch depth
  </div>
  <div class="rule-quick__section">
    <h4>Fix</h4>
    Extract Functions
  </div>
</aside>

---

## Why It Matters

Complex Blueprints are **development bottlenecks**:

| Problem | Impact |
|---------|--------|
| Maintainability | Harder to understand, modify, debug |
| Iteration speed | 500 nodes = ~2.5s compile |
| Defect rate | Bug fixes introduce new bugs |
| Version control | Merge conflicts on spatial data |

**Complexity score formula**:
```
Nodes × 1.0 + Branches × 5.0 + Depth × 6.0 + ...
```

<ExampleToggle
  problemId="bp-cmplx-problem"
  fixId="bp-cmplx-fix"
  problemText={bpCmplxRuleExamples[0].blueprintText}
  fixText={bpCmplxRuleExamples[1].blueprintText}
/>

---

## When It's Acceptable

- **Utility Blueprints**: Pure data-flow utilities rarely modified
- **Generated graphs**: Auto-generated content
- **Single complex algorithm**: Can't be decomposed further

---

## The Fix

### Option 1: Extract Functions

```
Before:
  Event BeginPlay → [20 init nodes] → [15 UI nodes] → [25 input nodes]

After:
  Event BeginPlay → InitializeCharacter → SetupUI → ConfigureInput
```

5-15 nodes per function is typical.

### Option 2: Flatten Branches

```
Before: Branch → Branch → Branch → Branch → Action
After:  Guard Branch → Return, Guard Branch → Return, Action
```

Early returns flatten the graph.

### Option 3: Macros for Patterns

Repeated node patterns → Collapse to Macro (compiles inline, no overhead).

---

## Compile Time Impact

| Nodes | Cold Compile | Warm Compile |
|-------|--------------|--------------|
| 50 | ~0.1s | ~0.05s |
| 200 | ~0.5s | ~0.15s |
| 500 | ~2.5s | ~0.3s |
| 1000+ | ~8-15s | ~1-2s |

Extracting functions improves incremental compilation 50-70%.

---

## Configuration

```
Project Settings → Blueprint Health Analyzer → BP-CMPLX-001 → 500.0
```

---

## Related

- [BP-TICK-001](/blueprint-health-analyzer/docs/rules/bp-tick-001/) — Complex graphs often have heavy Tick paths
- [BP-CAST-001](/blueprint-health-analyzer/docs/rules/bp-cast-001/) — Cast chains add complexity

<style>
{`
  .rule-meta {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 0.5rem;
  }

  .rule-title {
    font-size: 1.1rem;
    font-weight: 500;
    color: var(--color-text);
    margin-left: 0.25rem;
  }

  .category {
    display: inline-block;
    padding: 0.25rem 0.6rem;
    font-size: 0.75rem;
    font-weight: 500;
    background: var(--color-surface-muted);
    color: var(--color-text-muted);
    border-radius: 4px;
  }

  .rule-summary {
    font-size: 1.05rem;
    color: var(--color-text-muted);
    line-height: 1.6;
    margin-bottom: 1.5rem;
  }

  .rule-quick {
    display: flex;
    gap: 2rem;
    padding: 1rem 1.25rem;
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: 8px;
    margin-bottom: 1.5rem;
  }

  .rule-quick__section h4 {
    margin: 0 0 0.25rem 0;
    font-size: 0.7rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    color: var(--color-text-muted);
  }

  .rule-quick__section code,
  .rule-quick__section strong {
    font-size: 0.9rem;
    color: var(--color-text);
  }

  @media (max-width: 600px) {
    .rule-quick {
      flex-direction: column;
      gap: 1rem;
    }
  }
`}
</style>
