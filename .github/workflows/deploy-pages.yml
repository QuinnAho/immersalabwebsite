name: GitHub Pages Disabled

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest

    steps:
      - name: Explain deployment source
        run: echo "This repository deploys via Cloudflare Pages. GitHub Pages is intentionally disabled."
